import React from 'react'

export const Button = ({ styles }) => {
  return (
    <button className={`${styles} py-4 px-6 bg-blue-gradient rounded-md font-poppins font-medium text-primary outline-none`}>Get Started</button>
  )
}
